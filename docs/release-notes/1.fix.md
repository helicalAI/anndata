Restrict `anndata.utils.import_name` to an allowlist of root packages ({user}`oriolpetithelical`) as defense-in-depth against dynamic imports, and suppress false-positive semgrep alerts on non-literal imports and `__annotations__` usage.
